apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: redis-helm
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://charts.bitnami.com/bitnami
    chart: redis
    targetRevision: 17.11.3
    helm:
      releaseName: redis-helm
  destination:
    server: https://kubernetes.default.svc
    namespace: redis
  syncPolicy:
    automated: {}
    syncOptions:
    - CreateNamespace=true
